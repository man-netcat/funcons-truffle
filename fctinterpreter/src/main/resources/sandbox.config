general {
    funcon-term:
    initialise-binding
    scope(
        {"n"|->10},
        fold-left(
            int-add tuple-elements given,
            0,
            integer-sequence(1, bound-value "n")
        )
    );
}

tests {
    result-term: true;
}
